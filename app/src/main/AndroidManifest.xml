<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ac.id.binus.jobcancy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditProfileActivity"></activity>
        <activity android:name=".EditProfileEmployerActivity" />
        <activity android:name=".ForgotPasswordEmployerActivity" />
        <activity android:name=".ProfileApplyEmployeeActivity" />
        <activity android:name=".HomeEmployeeActivity" />
        <activity android:name=".HomeEmployerActivity" />
        <activity android:name=".ForgotPasswordEmployeeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".CompanyDetailActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>